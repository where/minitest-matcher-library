# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
sqlite3:
  adapter: sqlite3
  database: test/db/test.sqlite3
  pool: 5
  timeout: 5000
